name: Docker Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly at 3 AM UTC (aligns with dependency scans)
    - cron: '0 3 * * 3'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  packages: read

jobs:
  container-scan:
    name: Container Image Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Create placeholder database for CI build
        run: mkdir -p data && touch data/database.db

      - name: Build Docker image for scanning
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          context: .
          file: Dockerfile
          push: false
          load: true
          tags: finnish-law-mcp:scan
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner (SARIF)
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.35.0
        with:
          image-ref: finnish-law-mcp:scan
          format: 'sarif'
          output: 'trivy-container.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          vuln-type: 'os,library'
          ignore-unfixed: false
          exit-code: '0'

      - name: Upload Trivy SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@7fc1baf373eb073c686865bd453d412d506a05a2 # v3
        if: always()
        with:
          sarif_file: 'trivy-container.sarif'
          category: 'trivy-container'

      - name: Run Trivy for JSON report (audit evidence)
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.35.0
        with:
          image-ref: finnish-law-mcp:scan
          format: 'json'
          output: 'trivy-container.json'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          vuln-type: 'os,library'

      - name: Run Trivy for table output (summary)
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.35.0
        with:
          image-ref: finnish-law-mcp:scan
          format: 'table'
          severity: 'CRITICAL,HIGH'
          vuln-type: 'os,library'

      - name: Upload container scan artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: container-scan-results
          path: |
            trivy-container.sarif
            trivy-container.json
          retention-days: 90

  sbom-generation:
    name: Generate SBOM (Software Bill of Materials)
    runs-on: ubuntu-latest
    needs: container-scan
    if: always() && !cancelled()

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Create placeholder database for CI build
        run: mkdir -p data && touch data/database.db

      - name: Build image for SBOM
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          context: .
          file: Dockerfile
          push: false
          load: true
          tags: finnish-law-mcp:sbom
          platforms: linux/amd64
          cache-from: type=gha

      - name: Generate SBOM (CycloneDX format)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          image: finnish-law-mcp:sbom
          format: cyclonedx-json
          output-file: sbom-finnish-law-mcp-cyclonedx.json
          artifact-name: sbom-cyclonedx

      - name: Generate SBOM (SPDX format)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          image: finnish-law-mcp:sbom
          format: spdx-json
          output-file: sbom-finnish-law-mcp-spdx.json
          artifact-name: sbom-spdx

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-finnish-law-mcp
          path: |
            sbom-finnish-law-mcp-cyclonedx.json
            sbom-finnish-law-mcp-spdx.json
          retention-days: 365

  security-summary:
    name: Security Scan Summary
    runs-on: ubuntu-latest
    needs: [container-scan, sbom-generation]
    if: always()

    steps:
      - name: Generate summary
        env:
          CONTAINER_RESULT: ${{ needs.container-scan.result }}
          SBOM_RESULT: ${{ needs.sbom-generation.result }}
        run: |
          echo "## Docker Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** finnish-law-mcp:scan" >> $GITHUB_STEP_SUMMARY
          echo "**Platform:** linux/amd64" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Container Scan | $CONTAINER_RESULT |" >> $GITHUB_STEP_SUMMARY
          echo "| SBOM Generation | $SBOM_RESULT |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- **Trivy SARIF/JSON**: 90-day retention (audit evidence)" >> $GITHUB_STEP_SUMMARY
          echo "- **SBOM (CycloneDX + SPDX)**: 365-day retention (compliance)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### About This Scan" >> $GITHUB_STEP_SUMMARY
          echo "This workflow scans the Finnish Law MCP **Docker container image** for:" >> $GITHUB_STEP_SUMMARY
          echo "- OS package vulnerabilities (Alpine base)" >> $GITHUB_STEP_SUMMARY
          echo "- Node.js dependency vulnerabilities" >> $GITHUB_STEP_SUMMARY
          echo "- Container configuration issues" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "For **dependency-only** scans (package.json), see the trivy.yml workflow." >> $GITHUB_STEP_SUMMARY
